# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata

# Sass
.sass-cache/

# Bundler (GitHub Pages resolves gems itself; lockfile isn't used by the build)
Gemfile.lock
.bundle/
vendor/

# OS / editor cruft
.DS_Store
Thumbs.db
