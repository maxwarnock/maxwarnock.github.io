source "https://rubygems.org"

# Matches the gem set GitHub Pages builds with, so local previews behave
# like production. See https://pages.github.com/versions/
gem "github-pages", group: :jekyll_plugins

# Required to run `bundle exec jekyll serve` on Ruby 3.0+
gem "webrick", "~> 1.8"
